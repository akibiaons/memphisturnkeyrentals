import React from "react";

type Props = {};

export default function projects({}: Props) {
  return (
    <div>
      {/* Projects page will just basically be a grid of cards with an image of the house, an overlay with the address and a buttono in the
        overlay to view project, this button will link to an individual projectLayout page which consists of the bedrooms, bethrooms, sq ft, year built before and after images   */}
      <h1>Projects</h1>
    </div>
  );
}
